#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always EXIT_SUCCESS
 */
int main(void)
{
	int EXIT_SUCCESS = 0;

	printf("%d\n", _strlen("My Dyn Lib"));
	return (EXIT_SUCCESS);
}
